<div class="container">
	<h1 class="text-center">Artifacts</h1>
	<div class="row">
	  <% unless @artifacts.empty? %>
	    <%= render @artifacts %>
	  <%else%>
	    <h1>Looks like there are no courses yet...</h1>
	  <%end%>
	</div>
</div>